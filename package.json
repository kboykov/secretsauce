{
  "name": "secretsauce",
  "version": "1.4.0",
  "private": true,
  "description": "Browser extension that detects exposed secrets, API keys and endpoints, with built-in recon tabs.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test test/*.test.mjs",
    "preview": "node dev/preview-server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "author": "K. Boykov",
  "license": "MIT"
}
